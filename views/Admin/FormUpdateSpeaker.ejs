<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update speaker[0]</title>
    <link rel="stylesheet" href="/css/Form.css">
</head>
<body>
    <div class="login-box">
        <h2>Update speaker[0]</h2>
        <form action="/updateEventspeaker/<%= speaker[0].id %>" method="post" enctype="multipart/form-data">
            <div class="user-box">
                <input type="text" name="nom" id="name" value="<%= speaker[0].nom %>" required>
                <label>Nom</label>
            </div>
            <div class="user-box">
                <input type="text" name="prenom" id="prenom" value="<%= speaker[0].prenom %>" required>
                <label>Prenom</label>
            </div>
            <div class="user-box">
                <input type="file" value="<%= speaker[0].image %>" name="image" id="image">
                <label for="image">Image:</label>
                <% if (speaker[0].image) { %>
                    <img src="<%= speaker[0].image %>" alt="<%= speaker[0].nom %>" width="100">
                <% } %>
            </div>
            <div class="user-box">
                <input type="text" name="description" id="description" value="<%= speaker[0].description %>" required>
                <label>Description</label>
            </div>
            <div class="user-box">
                <input type="text" name="event_id" id="event_id" value="<%= speaker[0].event_id %>" required>
                <label>Event ID</label>
            </div>
            <div class="user-box">
                <input type="email" name="email" id="email" value="<%= speaker[0].email %>" required>
                <label>Email</label>
            </div>
            <div class="user-box">
                <input type="text" name="phone" id="phone" value="<%= speaker[0].phone %>" required>
                <label>Phone</label>
            </div>
            <div class="user-box">
                <button type="submit">Update</button>
            </div>
        </form>
    </div>
</body>
</html>
